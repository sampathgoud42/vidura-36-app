[project]
name = "tradier-bot"
version = "1.0.0"
description = "Standalone Tradier options desk: executor bot, auto-traders, HOT scan, options flow, signal engines"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["backend"]
addopts = "-q"
