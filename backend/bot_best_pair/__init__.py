"""bot_best_pair -- the daily report's best ticker + signal pairs, auto-traded
by a process of its own.

    ./backend/bot_best_pair/bot_best_pair.sh            trade (Ctrl-C stops)
    backend\\bot_best_pair\\bot_best_pair.bat --check    check the setup, trade nothing

Settings: bot_best_pair.env beside this file. How it trades and how it stays
out of the desk's way: bot.py.
"""
