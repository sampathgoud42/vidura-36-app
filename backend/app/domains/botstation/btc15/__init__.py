"""BTC-15 engine logic that is worth testing without an account.

v6 lives here rather than in the vendored script so its entry rule -- three
majors agreeing, inside a timing window, inside a price band -- can be
exercised with no network, no credentials and no exchange.
"""
